include-file = $HOME/.config/polybar/includes

[bar/main]
; general stuff
monitor = ${env:MONITOR:}
wm-name = polybar

; position and width etc.
width = 100%
height = 28
bottom = true

; fonts
line-size = 2
font-0 = "Roboto Mono Nerd Font Mono:pixelsize=16;4"
font-1 = "Roboto Mono Nerd Font Mono:pixelsize=28;7"

; colors
tray-background = ${colors.bg-alt}
background = ${colors.bg}
foreground = ${colors.fg}

; modules
tray-position = right

modules-left = i3 right
modules-center = left date right
modules-right = left battery
