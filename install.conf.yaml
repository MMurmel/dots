- defaults:
    link:
      create : true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.gitconfig:
    ~/.config/i3:
    ~/.config/nvim:
    ~/.config/ranger:
    ~/.alacritty.yml:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
