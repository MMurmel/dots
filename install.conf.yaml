- defaults:
    link:
      create : true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.config/i3:
    ~/.config/nvim:
    ~/.alacritty.yml:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
